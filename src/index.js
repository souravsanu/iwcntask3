import React from "react";
import ReactDOM from "react-dom";
import ApiPostComponent from "./ApiPostComponent";

ReactDOM.render(
  <React.StrictMode>
    <ApiPostComponent />
  </React.StrictMode>,
  document.getElementById("root")
);
